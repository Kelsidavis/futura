[package]
name = "rust_strings"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"

[lib]
crate-type = ["staticlib"]

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
codegen-units = 1
strip = true
