[package]
name = "apple_uart"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
description = "Apple Silicon S5L UART driver for Futura OS (ARM64)"

[lib]
crate-type = ["staticlib"]

[dependencies]
