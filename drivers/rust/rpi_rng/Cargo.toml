[package]
name = "rpi_rng"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
description = "Raspberry Pi BCM2711 hardware random number generator driver"

[lib]
crate-type = ["staticlib"]

[dependencies]
common = { path = "../common" }
