[package]
name = "apple_rtkit"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
description = "Apple RTKit co-processor IPC driver for Futura OS (ARM64)"

[lib]
crate-type = ["staticlib"]

[dependencies]
